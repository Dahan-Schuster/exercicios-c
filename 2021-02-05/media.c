#include <stdio.h>

int main()
{
    // como programador, sei que esse código está bem porco, mas não quero
    // colocar o carro na frente dos bois e sair usando for, entrada de dados etc
    // num exercício da segunda aula rsrs

    int valor1 = 42, valor2 = 7, valor3 = 65, valor4 = 14, valor5 = 9;
    float media = (valor1 + valor2 + valor3 + valor4 + valor5) / 5.0;
    printf("A média entre %d, %d, %d, %d e %d é: %f", valor1, valor2, valor3, valor4, valor5, media);

    return 0;
}
