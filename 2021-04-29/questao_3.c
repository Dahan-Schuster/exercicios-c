#include "lista_20.h"

#define N 4

void geraMatrizInca (int matriz [N][N])
{

    int i, j;
    int tam = N;

    // se (j == N - 1) i++
    // se (i == N - 1 && j == i) incI = +0, incJ = -1
    // se (i == N - 1 && j == 0) incI = -1, incJ = +1
}